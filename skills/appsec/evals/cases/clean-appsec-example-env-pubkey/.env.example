# Copy to .env and fill in. Never commit the filled-in copy.
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/orders
SESSION_SIGNING_KEY=generate-with-openssl-rand-hex-32
SMTP_PASSWORD=your-smtp-password-here
FEATURE_FLAG_NEW_CHECKOUT=false
